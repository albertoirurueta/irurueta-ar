= Self-calibration
:stem: latexmath
:description: How irurueta-ar's self-calibration recovers camera intrinsics with no calibration object, using the dual absolute quadric and Kruppa equations.
:keywords: self-calibration, dual absolute quadric, Kruppa equations, image of absolute conic, projective to metric, camera intrinsics, auto-calibration

NOTE: This documentation was generated with the assistance of AI. Please report any inaccuracies.

When a scene is reconstructed from image correspondences alone, without any known camera intrinsics, the
result lives in an arbitrary **projective stratum**: cameras and 3D points are only defined up to an unknown
stem:[4\times 4] projective transformation. *Self-calibration* recovers enough information about the cameras'
intrinsic parameters to upgrade that reconstruction to a **metric stratum** -- correct up to a similarity
(scale, rotation, translation) -- with no calibration object required. This page covers the two methods
`irurueta-ar` implements: the **dual absolute quadric** (recommended) and the **Kruppa equations** (simpler,
but more prone to degeneracies).

== The absolute dual quadric and the dual image of the absolute conic

The plane at infinity stem:[\boldsymbol{\pi}_\infty] and the absolute conic on it are invariant under similarity
transformations but not under general projective ones; together they fully characterize the metric stratum.
Their projective-invariant algebraic encoding is the **absolute dual quadric** stem:[\mathbf{Q}_\infty^{*}], which in the
metric stratum is simply

[stem]
++++
\mathbf{Q}_\infty^{*} = \begin{bmatrix} \mathbf{I}_{3\times 3} & 0 \\ 0^{T} & 0 \end{bmatrix}
++++

Projecting it into any view gives the **dual image of the absolute conic** (DIAC) stem:[\boldsymbol{\omega}_\infty^{*} = \mathbf{P}\, \mathbf{Q}_\infty^{*}\, \mathbf{P}^{T}]: in the metric stratum this reduces to stem:[\boldsymbol{\omega}_\infty^{*} = \mathbf{K} \mathbf{K}^{T}], a real
symmetric, positive-definite matrix. This is the key fact self-calibration exploits both ways: *estimating*
stem:[\mathbf{Q}_\infty^{*}] (or stem:[\boldsymbol{\omega}_\infty^{*}] per view) recovers stem:[\mathbf{K}] via a Cholesky factorization of the
DIAC xref:reference.adoc#ref-thesis[Irurueta, PhD thesis] xref:attachment$irurueta-phd-thesis.pdf#page=173[&sect;5.2].

Maps to `com.irurueta.ar.calibration.DualAbsoluteQuadric`, `DualImageOfAbsoluteConic` (its Cholesky-derived
intrinsics via `getIntrinsicParameters()`), and `ImageOfAbsoluteConic` (the non-dual form, projected instead
from the absolute conic itself).

== Calibration using the dual absolute quadric

Assuming some combination of zero skew, a principal point at the image origin, and unit aspect ratio (all
common and reasonable for consumer cameras), the DIAC in each view reduces to
stem:[\mathrm{diag}(\alpha^2,\alpha^2,1)] -- three off-diagonal zeros plus one equality constraint per view,
each linear in the unknown entries of stem:[\mathbf{Q}_\infty^{*}] once expressed through the projective camera matrices
stem:[\mathbf{P}_i = [\mathbf{R}_i\,|\,\mathbf{t}_i\]] of that (otherwise arbitrary-projective) stratum:

[stem]
++++
\mathbf{p}_i^{1T} \mathbf{Q}_\infty^{*} \mathbf{p}_i^{2} = 0,\quad
\mathbf{p}_i^{1T} \mathbf{Q}_\infty^{*} \mathbf{p}_i^{3} = 0,\quad
\mathbf{p}_i^{2T} \mathbf{Q}_\infty^{*} \mathbf{p}_i^{3} = 0,\quad
\mathbf{p}_i^{1T} \mathbf{Q}_\infty^{*} \mathbf{p}_i^{1} = \mathbf{p}_i^{2T} \mathbf{Q}_\infty^{*} \mathbf{p}_i^{2}
++++

Stacking these across at least 3 views (the reference camera stem:[\mathbf{P}_1 = [\mathbf{I}\,|\,0\]] alone gives a degenerate
system) yields a homogeneous linear system solved by the SVD null-space, exactly as in the
xref:epipolar/fundamental-matrix.adoc[fundamental matrix's linear estimation]
xref:reference.adoc#ref-thesis[Irurueta, PhD thesis] xref:attachment$irurueta-phd-thesis.pdf#page=176[&sect;5.3.1]. From the recovered
stem:[\mathbf{Q}_\infty^{*}], the focal length of each view falls out of its diagonal DIAC entry, and the plane at
infinity's direction stem:[\boldsymbol{\pi}_\infty] falls out of stem:[\mathbf{Q}_\infty^{*}]'s last row/column -- together giving the
projective-to-metric transform

[stem]
++++
\mathbf{T}_{PM} = \begin{bmatrix} \mathbf{K}^{-1} & \mathbf{0} \\ \boldsymbol{\pi}_\infty^{T} & 1 \end{bmatrix}
++++

(using either a per-view stem:[\mathbf{K}_i], or the first view's / the mean stem:[\mathbf{K}] if intrinsics are assumed constant
across the sequence) xref:reference.adoc#ref-thesis[Irurueta, PhD thesis] xref:attachment$irurueta-phd-thesis.pdf#page=182[&sect;5.3.1.1-5.3.1.2]. Applying
stem:[\mathbf{T}_{PM}] to every triangulated point upgrades the whole reconstruction to metric; applying stem:[\mathbf{T}_{PM}^{-1}]
upgrades the camera matrices.

Maps to `com.irurueta.ar.calibration.estimators.DualAbsoluteQuadricEstimator` (`LMSEDualAbsoluteQuadricEstimator`,
`WeightedDualAbsoluteQuadricEstimator`, or the robust family
`{RANSAC,LMedS,MSAC,PROSAC,PROMedS}DualAbsoluteQuadricRobustEstimator` following
xref:estimation/robust-vs-nonrobust.adoc[]), with `setZeroSkewness`, `setPrincipalPointAtOrigin`, and
`setFocalDistanceAspectRatioKnown` controlling exactly which of the assumptions above are enforced.

=== Enforcing the rank-3 constraint

A general stem:[4\times 4] symmetric quadric has 10 independent entries, so `DualAbsoluteQuadricEstimator`
actually solves for the full 10-parameter vector stem:[\mathbf{q} = (a,b,c,d,e,f,g,h,i,j)] rather than the simplified
6-parameter form shown above; the zero-skew/unit-aspect-ratio/known-principal-point assumptions only fix
*which rows* of the linear system look like the equations above, not the size of stem:[\mathbf{q}] itself. With one
camera discarded as degenerate, each additional view still contributes the same handful of homogeneous linear
equations in stem:[\mathbf{q}], stacked into a matrix stem:[\mathbf{A}] and solved through its SVD stem:[\mathbf{A} = \mathbf{U} \mathbf{S} \mathbf{V}^{T}].

Because the Dual Absolute Quadric is only guaranteed to be singular (rank 3, as any real dual quadric of a
proper quadric surface must be) when the linear system leaves *exactly* one free scale, but a handful of views
under a partial assumption set can leave the last **two** singular vectors stem:[\mathbf{v}_8, \mathbf{v}_9] of stem:[\mathbf{V}]
unconstrained, the estimator does not simply take stem:[\mathbf{v}_9] as the answer. Instead it takes the general
solution as a linear combination stem:[\mathbf{q} = \mathbf{v}_8 + \alpha \mathbf{v}_9], substitutes it into the requirement
stem:[\det(\mathbf{Q}_\infty^{*}) = 0], and expands this determinant into a quartic polynomial in stem:[\alpha] --
exactly the same companion-matrix eigenvalue technique used for the
xref:epipolar/fundamental-matrix.adoc[7-point algorithm]'s cubic (there in 1 free parameter instead of 2, and
degree 3 instead of 4). Each real root of the quartic is evaluated and the one that best satisfies the DAQ's
algebraic constraints is kept as the final, singularity-enforced solution. This behavior is controlled by
`setSingularityEnforced` (`isSingularityEnforced`), on by default; disabling it skips this step and simply
returns the raw SVD null-space solution, which is faster but not guaranteed to be a valid (singular) quadric.

=== LMSE vs. weighted solutions

Both concrete non-robust estimators solve the same linear system, but differ in how they combine more views
than the strict minimum:

* `LMSEDualAbsoluteQuadricEstimator` stacks every provided camera's equations with equal weight and, when
  `setLMSESolutionAllowed` is enabled and more than the minimum number of cameras is supplied, takes the
  overdetermined least-mean-squared-error solution (the smallest-singular-value right singular vector of the
  full stacked stem:[\mathbf{A}]) instead of an exact minimal solution.
* `WeightedDualAbsoluteQuadricEstimator` instead accepts an explicit per-camera quality weight, sorts cameras by
  weight, and caps how many actually enter the linear system (`DEFAULT_MAX_CAMERAS = 50`). This matters once a
  reconstruction has accumulated far more views than are needed algebraically: rather than solving an
  ever-growing, equally-weighted system, only the most reliable (highest-weight) views are kept, bounding both
  the computational cost and the influence of poorly conditioned cameras.

The robust family (`{RANSAC,LMedS,MSAC,PROSAC,PROMedS}DualAbsoluteQuadricRobustEstimator`) wraps either
non-robust solver inside the same xref:estimation/robust-vs-nonrobust.adoc[outlier-rejection loop] used
everywhere else in this library, scoring each candidate DAQ by how well the *other* cameras (not in the current
minimal sample) satisfy its induced DIAC constraints -- useful when some estimated camera matrices in the
sequence are themselves inaccurate (e.g. from a poorly resectioned view).

== Estimating the image of the absolute conic from homographies

A complementary route to the same DIAC/IAC relationship uses 2D homographies instead of full camera matrices,
and is what `com.irurueta.ar.calibration.estimators.ImageOfAbsoluteConicEstimator` implements. Given a
homography stem:[\mathbf{H} = (\mathbf{h}_1\ \mathbf{h}_2\ \mathbf{h}_3)] mapping a reference plane's points into a view, the IAC
stem:[\boldsymbol{\omega}_\infty] satisfies two constraints per homography (the same ones underlying Zhang's calibration
method):

[stem]
++++
\mathbf{h}_1^{T}\, \boldsymbol{\omega}_\infty\, \mathbf{h}_2 = 0,\qquad \mathbf{h}_1^{T}\, \boldsymbol{\omega}_\infty\, \mathbf{h}_1 = \mathbf{h}_2^{T}\, \boldsymbol{\omega}_\infty\, \mathbf{h}_2
++++

The IAC has 5 independent entries (up to scale), so in general stem:[2n \le 5 \Rightarrow n \le 2.5], i.e. at
least 3 homographies are needed. Each simplifying assumption removes one unknown and one required homography:
assuming zero skew needs only stem:[2n+1 \le 5 \Rightarrow n \le 2] homographies, and further assuming a
principal point at the origin needs only stem:[2n+3 \le 5 \Rightarrow n \le 1] -- a *single* homography
suffices, controlled by the same `setZeroSkewness`/`setPrincipalPointAtOrigin` style flags as the DAQ estimator.
Once stem:[\boldsymbol{\omega}_\infty] is known, intrinsics follow from a Cholesky-related factorization exactly as for its
dual, the DIAC (&sect;"The absolute dual quadric..." above).

This is the same machinery that underlies xref:calibration/camera-calibration.adoc[pattern-based calibration]
(where stem:[\mathbf{H}] maps a known calibration pattern's ideal points into each photographed view) -- the only
difference from *self*-calibration is where the homographies come from: a physical pattern there, versus a
plane-induced homography between two already-reconstructed (projective) views here, or the DAQ-based
projective camera matrices above. `com.irurueta.ar.calibration.estimators.LMSEImageOfAbsoluteConicEstimator` and
`WeightedImageOfAbsoluteConicEstimator` mirror the DAQ estimator's LMSE-vs-weighted distinction, and
`{RANSAC,LMedS,MSAC,PROSAC,PROMedS}ImageOfAbsoluteConicRobustEstimator` again add outlier rejection over a
larger set of homographies following xref:estimation/robust-vs-nonrobust.adoc[].

== The Kruppa equations

image::calibration/kruppa-tangency.svg[Epipolar tangency to a conic relates the Kruppa equations,600,role=text-center]

The Kruppa equations are the historically earlier self-calibration method, and only need a *pair* of views'
fundamental matrix -- no full camera matrices required. They come from epipolar tangency: the two epipolar
lines from an epipole tangent to a conic map, under the epipolar geometry, to the two tangent lines from the
other epipole tangent to the corresponding conic in the other view. Applying this to the absolute conic itself
(so the conics become the DIACs stem:[\boldsymbol{\omega}_\infty^{*}], stem:[\boldsymbol{\omega}_\infty^{*\prime}] of each view) gives

[stem]
++++
\mathbf{F}^{T}\, \boldsymbol{\omega}_\infty^{*\prime}\, \mathbf{F} = [\mathbf{e}]_\times\, \boldsymbol{\omega}_\infty^{*}\, [\mathbf{e}]_\times
++++

which, substituting the SVD stem:[\mathbf{F} = \mathbf{U}\,\mathrm{diag}(\sigma_1,\sigma_2,0)\,\mathbf{V}^{T}] (so stem:[\mathbf{e}=\mathbf{v}_3],
stem:[\mathbf{e}'=\mathbf{u}_3]), reduces to the classic three-way equality relating entries of both DIACs to
stem:[\sigma_1,\sigma_2] xref:reference.adoc#ref-thesis[Irurueta, PhD thesis] xref:attachment$irurueta-phd-thesis.pdf#page=183[&sect;5.3.2]:

[stem]
++++
\frac{\mathbf{u}_2^{T}\boldsymbol{\omega}_\infty^{*\prime}\mathbf{u}_2}{\sigma_1^{2}\,\mathbf{v}_1^{T}\boldsymbol{\omega}_\infty^{*}\mathbf{v}_1}
= -\frac{\mathbf{u}_1^{T}\boldsymbol{\omega}_\infty^{*\prime}\mathbf{u}_2}{\sigma_1\sigma_2\,\mathbf{v}_1^{T}\boldsymbol{\omega}_\infty^{*}\mathbf{v}_2}
= \frac{\mathbf{u}_1^{T}\boldsymbol{\omega}_\infty^{*\prime}\mathbf{u}_1}{\sigma_2^{2}\,\mathbf{v}_2^{T}\boldsymbol{\omega}_\infty^{*}\mathbf{v}_2}
++++

Under the same zero-skew/unit-aspect-ratio/known-principal-point assumptions as above, this gives enough
equations to solve for the (constant) focal length from a *single* pair of views -- fewer views than the dual
absolute quadric method needs -- but it only recovers the DIAC, not the plane at infinity, so it is more
sensitive to degenerate configurations and is used here as a fallback rather than the default
xref:reference.adoc#ref-thesis[Irurueta, PhD thesis] xref:attachment$irurueta-phd-thesis.pdf#page=183[&sect;5.3.2], xref:attachment$irurueta-phd-thesis.pdf#page=187[&sect;5.5].

Maps to `com.irurueta.ar.calibration.estimators.KruppaDualImageOfAbsoluteConicEstimator`.

== Critical motion sequences

Both methods can degenerate for certain camera motions: pure rotation about a single axis, too little
translation between views, or a narrow field of view all reduce the rank of the linear systems above and make
the recovered stem:[\mathbf{K}]/stem:[\boldsymbol{\pi}_\infty] unreliable. General, wide camera motion (translation in more than one
direction, varied viewing angles) avoids these critical sequences
xref:reference.adoc#ref-thesis[Irurueta, PhD thesis] xref:attachment$irurueta-phd-thesis.pdf#page=187[&sect;5.4]. The more assumptions you can safely make about
stem:[\mathbf{K}] up front (zero skew, known principal point, unit aspect ratio), the fewer free parameters remain to
absorb such degeneracies.

== Example

[source,java]
----
// Dual absolute quadric self-calibration from >= 3 projective cameras
var daqEstimator = DualAbsoluteQuadricEstimator.create(projectiveCameras);
daqEstimator.setZeroSkewness(true);
daqEstimator.setPrincipalPointAtOrigin(true);
daqEstimator.setFocalDistanceAspectRatioKnown(true);
DualAbsoluteQuadric daq = daqEstimator.estimate();

// Kruppa equations from a single pair of views (only needs F)
var kruppaEstimator = new KruppaDualImageOfAbsoluteConicEstimator(fundamentalMatrix);
DualImageOfAbsoluteConic diac = kruppaEstimator.estimate();
PinholeCameraIntrinsicParameters intrinsics = diac.getIntrinsicParameters();
----

== Key classes

[cols="2,1,1",options="header"]
|===
|Class |Repository |Javadoc

|`DualAbsoluteQuadricEstimator`
|https://github.com/albertoirurueta/irurueta-ar/blob/master/src/main/java/com/irurueta/ar/calibration/estimators/DualAbsoluteQuadricEstimator.java[Source]
|https://albertoirurueta.github.io/irurueta-ar/mvn-site/apidocs/com/irurueta/ar/calibration/estimators/DualAbsoluteQuadricEstimator.html[Javadoc]

|`DualAbsoluteQuadric`
|https://github.com/albertoirurueta/irurueta-ar/blob/master/src/main/java/com/irurueta/ar/calibration/DualAbsoluteQuadric.java[Source]
|https://albertoirurueta.github.io/irurueta-ar/mvn-site/apidocs/com/irurueta/ar/calibration/DualAbsoluteQuadric.html[Javadoc]

|`KruppaDualImageOfAbsoluteConicEstimator`
|https://github.com/albertoirurueta/irurueta-ar/blob/master/src/main/java/com/irurueta/ar/calibration/estimators/KruppaDualImageOfAbsoluteConicEstimator.java[Source]
|https://albertoirurueta.github.io/irurueta-ar/mvn-site/apidocs/com/irurueta/ar/calibration/estimators/KruppaDualImageOfAbsoluteConicEstimator.html[Javadoc]

|`DualImageOfAbsoluteConic`
|https://github.com/albertoirurueta/irurueta-ar/blob/master/src/main/java/com/irurueta/ar/calibration/DualImageOfAbsoluteConic.java[Source]
|https://albertoirurueta.github.io/irurueta-ar/mvn-site/apidocs/com/irurueta/ar/calibration/DualImageOfAbsoluteConic.html[Javadoc]
|===

== Next

See xref:sfm/structure-from-motion.adoc[] for how self-calibration fits into the full reconstruction pipeline:
projective reconstruction first, self-calibration second, metric reconstruction last.

== References

xref:reference.adoc#ref-thesis[Irurueta, PhD thesis] xref:attachment$irurueta-phd-thesis.pdf#page=37[&sect;1.4.3], xref:attachment$irurueta-phd-thesis.pdf#page=173[&sect;5] +
xref:reference.adoc#ref-hartley-zisserman-2003[Hartley & Zisserman, 2003] chapter 19 ("Auto-Calibration") +
xref:reference.adoc#ref-hartley-zisserman-2003[Hartley & Zisserman, 2003] &sect;3.6-3.7 (absolute conic / dual
quadric)
