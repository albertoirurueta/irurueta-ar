= Fundamental matrix estimation
:stem: latexmath

NOTE: This documentation was generated with the assistance of AI. Please report any inaccuracies.

The fundamental matrix stem:[\mathbf{F}] is a stem:[3\times 3], rank-2 matrix that encodes the epipolar geometry between
a pair of uncalibrated views: it is the only piece of information needed to constrain where a point in one
image can match in the other, without knowing anything about the cameras' intrinsic parameters.

image::epipolar/epipolar-geometry.svg[Epipolar geometry between two views,600,role=text-center]

Given a 3D world point stem:[\mathbf{M}], its projections stem:[\mathbf{m}] and stem:[\mathbf{m}'] on the left and right views, the
camera centers stem:[\mathbf{C}], stem:[\mathbf{C}'] and stem:[\mathbf{M}] always define a plane -- the *epipolar plane* stem:[\boldsymbol{\Pi}_e].
Its intersection with each image plane is an *epipolar line* (stem:[\mathbf{l}] on the left view, stem:[\mathbf{l}'] on the
right), and the projection of one camera's center onto the other view is that view's *epipole* (stem:[\mathbf{e}],
stem:[\mathbf{e}']). As stem:[\mathbf{M}] varies, every epipolar plane still contains the baseline stem:[\mathbf{C}\mathbf{C}'], so all epipolar
lines in a view pass through that view's epipole -- a pencil of lines:

image::epipolar/pencil-of-epipolar-lines.svg[Pencil of epipolar planes and lines,600,role=text-center]

== The epipolar constraint

Because stem:[\mathbf{m}] lies on epipolar line stem:[\mathbf{l}] and stem:[\mathbf{l}] passes through epipole stem:[\mathbf{e}], stem:[\mathbf{l} \equiv [\mathbf{e}\]_\times \mathbf{m}], where stem:[[\mathbf{e}\]_\times] is the skew-symmetric cross-product matrix of stem:[\mathbf{e}]. Back-projecting
epipolar lines to the (shared) epipolar plane and forward-projecting to the other view yields the linear map

[stem]
++++
\mathbf{l}' \equiv \left(\mathbf{P}'^{T}\right)^{\dagger} \mathbf{P}^{T} [\mathbf{e}]_\times \mathbf{m} = \mathbf{F} \mathbf{m},\qquad \mathbf{F} \equiv [\mathbf{e}']_\times\, \mathbf{P}' \mathbf{P}^{\dagger}
++++

so any matched pair must satisfy the **epipolar constraint**

[stem]
++++
\mathbf{m}'^{T} \mathbf{F}\, \mathbf{m} = 0
++++

stem:[\mathbf{F}] is defined up to scale (8 DOF from stem:[3\times 3 - 1]) and, since stem:[\mathbf{F}\mathbf{e} = 0] and stem:[\mathbf{e}'^{T}\mathbf{F} = 0], its left/right null-spaces are exactly the two epipoles -- forcing stem:[\det(\mathbf{F}) = 0] and leaving 7 true
degrees of freedom. Given the SVD stem:[\mathbf{F} = \mathbf{U} \mathbf{S} \mathbf{V}^{T}], the epipoles are recovered directly as the singular
vectors of the zero singular value: stem:[\mathbf{e} = \mathbf{v_3}] (last column of stem:[\mathbf{V}]), stem:[\mathbf{e}' = \mathbf{u}_3] (last column of
stem:[\mathbf{U}]) xref:reference.adoc#ref-thesis[Irurueta, PhD thesis] xref:attachment$irurueta-phd-thesis.pdf#page=103[&sect;3.2.1]
(`FundamentalMatrix.computeEpipoles()`, `getLeftEpipole()`/`getRightEpipole()`). When the pair of cameras
stem:[\mathbf{P}], stem:[\mathbf{P}'] is already known, `new FundamentalMatrix(leftCamera, rightCamera)` computes stem:[\mathbf{F}]
directly from `stem:[\mathbf{F} = [\mathbf{e}'\]_\times \mathbf{P}'\mathbf{P}^{\dagger}]`.

== Linear estimation and the 8-point algorithm

Expanding the constraint for inhomogeneous coordinates gives one linear equation per correspondence in the 9
unknowns of stem:[\mathbf{F}] stacked as a vector stem:[\mathbf{f}]:

[stem]
++++
\left(x x',\ y x',\ x',\ x y',\ y y',\ y',\ x,\ y,\ 1\right)\, \mathbf{}f = 0 \;\Longrightarrow\; \mathbf{A} \mathbf{f} = 0
++++

With 8 or more correspondences, stem:[\mathbf{f}] is the right singular vector of stem:[\mathbf{A}] for its smallest singular
value (exact null-space for exactly 8 points, LMSE fit for more). The resulting matrix is not guaranteed to
have rank 2, so the *8-point algorithm* enforces it by zeroing the smallest singular value of stem:[\mathbf{F}]'s own
SVD: if stem:[\mathbf{F} = \mathbf{U}\,\mathrm{diag}(s_1,s_2,s_3)\,\mathbf{V}^{T}], the closest rank-2 matrix in Frobenius norm is
stem:[\mathbf{F}' = \mathbf{U}\,\mathrm{diag}(s_1,s_2,0)\,\mathbf{V}^{T}]. Point coordinates are normalized beforehand (centroid at the
origin, RMS distance stem:[\sqrt2]) to keep the linear system well conditioned
xref:reference.adoc#ref-thesis[Irurueta, PhD thesis] xref:attachment$irurueta-phd-thesis.pdf#page=108[&sect;3.2.2.1], matching the normalized-DLT practice in
Hartley & Zisserman xref:reference.adoc#ref-hartley-zisserman-2003[Hartley & Zisserman, 2003] &sect;11.2.

Maps to `com.irurueta.ar.epipolar.estimators.EightPointsFundamentalMatrixEstimator` (`setPointsNormalized`
toggles normalization).

== The 7-point algorithm

Seven correspondences make stem:[\mathbf{A}] a stem:[7\times 9] matrix whose null-space has dimension 2, spanned by
stem:[\mathbf{F}'] and stem:[\mathbf{F}''] (the two smallest-singular-value right singular vectors). The true stem:[\mathbf{F}] is some
combination stem:[\mathbf{F} = \mathbf{F}' + \alpha \mathbf{F}'']; enforcing the rank-2 constraint stem:[\det(\mathbf{F}' + \alpha \mathbf{F}'') = 0] gives a
cubic in stem:[\alpha] whose real root(s) are found via the companion-matrix/eigenvalue technique. One real
root means a unique, non-degenerate solution; three distinct real roots signal a geometrically degenerate
configuration (e.g. all points near-coplanar, or the two camera centers too close together)
xref:reference.adoc#ref-thesis[Irurueta, PhD thesis] xref:attachment$irurueta-phd-thesis.pdf#page=110[&sect;3.2.2.2].

Maps to `SevenPointsFundamentalMatrixEstimator` (`estimateAll()` returns every real solution; `estimate()`
picks the single valid one).

Two additional non-robust variants exist for restricted motions: `AffineFundamentalMatrixEstimator` (for
affine cameras) and `PlanarFundamentalMatrixEstimator` (when all points lie on a common plane, using the
plane-induced homography relation stem:[\mathbf{F} = [\mathbf{e}'\]_\times \mathbf{H}]).

== Robust estimation

With many, possibly mismatched correspondences, a robust wrapper repeatedly draws a minimal sample (7 or 8
points), fits stem:[\mathbf{F}] with the corresponding non-robust estimator, and scores the fit by the *epipolar
distance* of every correspondence to its generated epipolar line:

[stem]
++++
d(\mathbf{m}', \mathbf{l}') = \frac{\left|\mathbf{m}'^{T} \mathbf{l}'\right|}{\sqrt{l'^{2}_1 + l'^{2}_2}},\qquad \mathbf{l}' = \mathbf{F} \mathbf{m}
++++

This is exactly the two-tier pattern described in xref:estimation/robust-vs-nonrobust.adoc[]: pick
`FundamentalMatrixEstimatorMethod.SEVEN_POINTS_ALGORITHM` or `EIGHT_POINTS_ALGORITHM` as the inner non-robust
solver, and `RobustEstimatorMethod.{RANSAC,LMEDS,MSAC,PROSAC,PROMEDS}` as the outlier-rejection strategy. A
threshold of 1-2 pixels on stem:[d(\mathbf{m}',\mathbf{l}')] works well in practice
xref:reference.adoc#ref-thesis[Irurueta, PhD thesis] xref:attachment$irurueta-phd-thesis.pdf#page=116[&sect;3.2.3].

Maps to `{RANSAC,LMedS,MSAC,PROSAC,PROMedS}FundamentalMatrixRobustEstimator`, all subclasses of the abstract
`FundamentalMatrixRobustEstimator` (factory: `FundamentalMatrixRobustEstimator.create(...)`).

== Example

[source,java]
----
// Exact 7-point solve (7 correspondences)
var sevenPoint = new SevenPointsFundamentalMatrixEstimator(leftPoints, rightPoints);
List<FundamentalMatrix> candidates = sevenPoint.estimateAll(); // 1 or 3 real solutions

// Robust estimation over many (possibly wrong) correspondences
var ransac = (RANSACFundamentalMatrixRobustEstimator) FundamentalMatrixRobustEstimator.create(
        leftPoints, rightPoints, RobustEstimatorMethod.RANSAC);
ransac.setThreshold(1.0); // pixels of epipolar distance
FundamentalMatrix fundamentalMatrix = ransac.estimate();

Point2D leftEpipole = fundamentalMatrix.getLeftEpipole();
Point2D rightEpipole = fundamentalMatrix.getRightEpipole();
----

== Key classes

[cols="2,1,1",options="header"]
|===
|Class |Repository |Javadoc

|`FundamentalMatrix`
|https://github.com/albertoirurueta/irurueta-ar/blob/master/src/main/java/com/irurueta/ar/epipolar/FundamentalMatrix.java[Source]
|https://albertoirurueta.github.io/irurueta-ar/mvn-site/apidocs/com/irurueta/ar/epipolar/FundamentalMatrix.html[Javadoc]

|`SevenPointsFundamentalMatrixEstimator`
|https://github.com/albertoirurueta/irurueta-ar/blob/master/src/main/java/com/irurueta/ar/epipolar/estimators/SevenPointsFundamentalMatrixEstimator.java[Source]
|https://albertoirurueta.github.io/irurueta-ar/mvn-site/apidocs/com/irurueta/ar/epipolar/estimators/SevenPointsFundamentalMatrixEstimator.html[Javadoc]

|`FundamentalMatrixRobustEstimator`
|https://github.com/albertoirurueta/irurueta-ar/blob/master/src/main/java/com/irurueta/ar/epipolar/estimators/FundamentalMatrixRobustEstimator.java[Source]
|https://albertoirurueta.github.io/irurueta-ar/mvn-site/apidocs/com/irurueta/ar/epipolar/estimators/FundamentalMatrixRobustEstimator.html[Javadoc]

|`RANSACFundamentalMatrixRobustEstimator`
|https://github.com/albertoirurueta/irurueta-ar/blob/master/src/main/java/com/irurueta/ar/epipolar/estimators/RANSACFundamentalMatrixRobustEstimator.java[Source]
|https://albertoirurueta.github.io/irurueta-ar/mvn-site/apidocs/com/irurueta/ar/epipolar/estimators/RANSACFundamentalMatrixRobustEstimator.html[Javadoc]
|===

== Next

xref:epipolar/essential-matrix.adoc[] shows how stem:[\mathbf{F}] combines with known intrinsic calibration to give the
essential matrix and an initial metric camera pair; xref:epipolar/point-correction.adoc[] shows how to correct
noisy correspondences so they lie exactly on the epipolar geometry before triangulating them.

== References

xref:reference.adoc#ref-thesis[Irurueta, PhD thesis] xref:attachment$irurueta-phd-thesis.pdf#page=102[&sect;3.2] +
xref:reference.adoc#ref-hartley-zisserman-2003[Hartley & Zisserman, 2003] chapters 9 and 11 +
xref:reference.adoc#ref-luong-faugeras-1995[Luong & Faugeras, 1995] +
xref:reference.adoc#ref-zhang-1998[Zhang, 1998] +
xref:reference.adoc#ref-csurka-1996[Csurka et al., 1996]
